# `cargo build`/`cargo run` from inside rtic/ target the NUCLEO-L476RG.  From the
# workspace root use `cargo build -p rtic-l476 --target thumbv7em-none-eabihf`.
[build]
target = "thumbv7em-none-eabihf"

[env]
DEFMT_LOG = "info"
