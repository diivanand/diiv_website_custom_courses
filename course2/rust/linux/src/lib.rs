//! Course 2 — embedded-Linux tier.
//!
//! Exercises are binaries in `src/bin/ex-M-N.rs`.  On the Mac:
//! `cargo check -p linux --target aarch64-unknown-linux-gnu` (compile only —
//! there is no cross linker here) and `cargo build -p linux` for the subset that
//! is POSIX-common with macOS.  On the Jetson / Pi: `cargo build --release`.
//! This library ships empty on purpose.
