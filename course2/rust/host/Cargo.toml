[package]
name = "host"
edition.workspace = true
version.workspace = true
publish.workspace = true
description = "Course 2 host tier — std Rust on the Mac: unit tests, Miri, Clippy"

[dependencies]
heapless = { workspace = true }

[lints.rust]
unsafe_op_in_unsafe_fn = "deny"
missing_docs = "warn"

[lints.clippy]
all = "warn"
